server:
  port: 8084
spring:
  application:
    name: consul-provider
  cloud:
    consul:
      host: 192.168.16.113    # consul 启动地址
      port: 8500         # consul 启动端口
      discovery:
        tags: tag1          # 指定服务的标签, 用逗号隔开
